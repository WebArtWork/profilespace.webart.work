import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'profilespace.webart.work',
  appName: 'Profile Space',
  webDir: 'dist/app/browser'
};

export default config;
