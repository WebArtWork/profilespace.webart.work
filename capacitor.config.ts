import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.appprofilespace.webart.work',
  appName: 'Profile Space',
  webDir: 'dist/app/browser',
};

export default config;
